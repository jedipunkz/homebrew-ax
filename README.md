# homebrew-ax

Homebrew tap for [ax](https://github.com/jedipunkz/ax) — Run multiple Claude Code agents in parallel, each isolated in its own git worktree, and monitor them all from a single terminal dashboard.

## Install

```sh
brew tap jedipunkz/ax
brew install ax
```

## Update

```sh
brew upgrade ax
```
